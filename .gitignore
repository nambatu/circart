# Dependencies
node_modules/

# Editor
.vscode/
.DS_Store

# Runtime artefacts from the pipeline
seen.json
game_*.json

# Capacitor native build output (the projects themselves are kept)
android/build/
android/app/build/
android/.gradle/
android/.idea/
android/local.properties
android/capacitor-cordova-android-plugins/
ios/App/Pods/
ios/App/build/
ios/DerivedData/
ios/App/output/

# Raw full-resolution images (~179 MB, three near-duplicate copies).
# These are regenerated by fetch_images.py; the web-sized versions the game
# actually serves live in docs/images and ARE committed.
# Delete these two lines if you want the originals in the repo as well.
www/images/
www-mobile/images/
